version: '3.8'
services:
  postgres:
    image: postgres:15
    container_name: job_db
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
      POSTGRES_DB: job_match_db
    ports:
      - "5432:5432"